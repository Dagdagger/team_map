<script type="text/javascript">
handler = Gmaps.build('Google');
handler.buildMap({
	provider: { mapTypeId: google.maps.MapTypeId.HYBRID, maxZoom: 5 },
	internal: {id: 'map'}},function(){
	var markersData = <%= raw @hash.to_json %>;
	updateMarkers(handler, markersData);
});
</script>
